{
  "name": "explosion-js-generator-demo",
  "version": "0.2.0",
  "description": "",
  "keywords": [
    "js-generator",
    "js",
    "generator"
  ],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
